{
  "name": "portfolio_v3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=packages/frontend",
    "build": "npm run build --workspace=packages/frontend",
    "start": "npm run start --workspace=packages/frontend",
    "lint": "npm run lint --workspace=packages/frontend",
    "test": "npm run test --workspaces --if-present",
    "synth": "npm run synth --workspace=packages/backend",
    "deploy": "npm run deploy --workspace=packages/backend",
    "diff": "npm run diff --workspace=packages/backend"
  },
  "workspaces": [
    "packages/*"
  ]
}
